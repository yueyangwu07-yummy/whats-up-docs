model_name: allenai/led-large-16384
output_dir: models/led_large_full
results_path: models/led_large_full/final_metrics.json
submission_path: submissions/led_large_submission.csv
data_dir: data
train_filename: train.csv
test_filename: test_features.csv
batch_size: 1
eval_batch_size: 1
gradient_accumulation_steps: 4
learning_rate: 3.0e-5
weight_decay: 0.01
epochs: 3
max_input_length: 16384
max_target_length: 256
generation_max_length: 256
num_beams: 4
tokenizer_padding: longest
train_subset_size:
eval_subset_size:
val_ratio: 0.1
warmup_ratio: 0.1
warmup_steps:
logging_steps: 25
seed: 42
save_total_limit: 1
lr_scheduler_type: linear
use_global_attention: true
fp16: true
early_stopping_patience: 2
early_stopping_threshold: 0.0
dataloader_num_workers: 2
label_smoothing_factor: 0.0
report_to: []
warmup_steps: 0
warmup_ratio: 0.0

gradient_checkpointing: true
per_device_train_batch_size: 1
per_device_eval_batch_size: 1
torch_compile: false




